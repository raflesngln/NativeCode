import React from "react";
import { Container,View,Text,Tab,Item,Input,Form,Label, Tabs, ScrollableTab,Body,Icon } from 'native-base';

const ContactList = ({ children }) => (
  <View>{children}</View>
);

export default ContactList;